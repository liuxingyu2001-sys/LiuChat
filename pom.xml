<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.liuxingyu</groupId>
  <artifactId>LiuChat</artifactId>
  <version>0.2.0</version>
  <packaging>jar</packaging>

  <name>LiuChat</name>
  <description>聊天插件 —— 自定义格式 / 禁言 / 冷却 / 重复检测 / 私聊（零第三方框架依赖）</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
    <spigot.version>1.21.11-R0.1-SNAPSHOT</spigot.version>
    <placeholderapi.version>2.11.6</placeholderapi.version>
    <sqlite.version>3.47.0.0</sqlite.version>
  </properties>

  <repositories>
    <repository>
      <id>spigotmc-repo</id>
      <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
    </repository>
    <repository>
      <id>placeholderapi</id>
      <url>https://repo.extendedclip.com/releases/</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.spigotmc</groupId>
      <artifactId>spigot-api</artifactId>
      <version>${spigot.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>me.clip</groupId>
      <artifactId>placeholderapi</artifactId>
      <version>${placeholderapi.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- 运行期由 plugin.yml 的 libraries（Paper 自动下载）/ 服务端自带驱动提供，不打进 jar -->
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>${sqlite.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- 单元测试（跨服消息编解码） -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- 产物名沿用本机惯例：Liu-LiuChat-0.1.0.jar -->
    <finalName>Liu-${project.artifactId}-${project.version}</finalName>
    <resources>
      <!-- 普通资源不做过滤：config.yml/messages.yml 里有 ${player} 这类内部占位符，
           绝不能让 maven 去碰；只有 plugin.yml 需要替换 ${project.version} -->
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
        <excludes>
          <exclude>plugin.yml</exclude>
        </excludes>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
        <includes>
          <include>plugin.yml</include>
        </includes>
        <filtering>true</filtering>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <encoding>${project.build.sourceEncoding}</encoding>
        </configuration>
      </plugin>
      <!-- 不用 shade：本插件零运行期依赖需要打包，保持 jar 最小 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <!-- Maven 自带的 2.17 不识别 JUnit 5，必须显式升级 -->
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>

</project>
